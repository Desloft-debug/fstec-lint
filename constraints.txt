# Версии инструментов разработки, на которых прогоняется CI.
#
# Без фиксации сборка ломается от чужого релиза (новое правило ruff,
# новая проверка mypy), а «прогон на такой-то версии» перестаёт быть
# воспроизводимым утверждением — для инструмента, отчёты которого идут
# в документы по оценке соответствия, это существенно.
#
# Обновляются отдельным коммитом, чтобы изменение поведения проверок
# было видно в истории.
pytest==9.1.1
pytest-cov==7.0.0
ruff==0.16.5
mypy==1.19.1
types-PyYAML==6.0.12.20260815
PyYAML==6.0.2
